<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--全屏显示，有透明状态栏-->
    <style name="statusBar" parent="AppTheme">
    </style>
    <style name="AppTheme.transparent" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
    <style name="UpdateUserEditText" parent="Theme.AppCompat.Light">
                <item name="colorControlNormal">@color/AEB3C0</item>
                <item name="colorControlActivated">@color/AEB3C0</item>
          </style>
    <!--全屏显示，无透明状态栏-->
    <style name="TranslucentTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--全屏显示，有透明状态栏-->
    <style name="Welcome.theme" parent="statusBar"></style>
    <!-- 自定义仿IOS的AlertDialog的样式 -->



    -<style parent="@android:style/Theme.Dialog" name="AlertDialogStyle">

    <item name="android:windowBackground">@android:color/transparent</item>

    <item name="android:windowContentOverlay">@null</item>

    <item name="android:windowIsFloating">true</item>

    <item name="android:windowFrame">@null</item>

    <item name="android:backgroundDimEnabled">true</item>

    <item name="android:windowNoTitle">true</item>

    <item name="android:windowIsTranslucent">true</item>

</style>
</resources>
